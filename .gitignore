# User data — never commit the database
data/*
!data/.htaccess

# Local dev config
.claude/
*.log

# Android build workspace — only the recipe is versioned.
# NEVER commit android.keystore / keystore-password.txt (signing identity).
android-app/*
!android-app/twa-manifest.json
